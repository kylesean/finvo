import 'package:flutter/material.dart';

/// Third-party brand colors (immutable, not theme-dependent).
///
/// These colors are defined by brand guidelines and must not change
/// with app theme switches.
abstract final class BrandColors {
  /// Alipay brand blue
  static const Color alipay = Color(0xFF1677FF);

  /// WeChat Pay brand green
  static const Color wechatPay = Color(0xFF07C160);
}
