# Client Release CI/CD Pipeline
# Automatically builds Android APK and iOS IPA in parallel upon tag push (v*) or manual dispatch,
# and attaches both binaries to a unified GitHub Release.

name: Client Release Build

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  FLUTTER_CHANNEL: 'stable'

jobs:
  # ============================================================================
  # Android Release Build
  # ============================================================================
  android:
    name: Android Release
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./client

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Flutter SDK
        uses: subosito/flutter-action@v2
        with:
          channel: ${{ env.FLUTTER_CHANNEL }}
          cache: true

      - name: Install Dependencies
        run: flutter pub get

      # Reconstruct the release keystore on the runner. The keystore itself
      # must NEVER be committed to the repo (it is the app's permanent
      # identity; a leaked keystore lets anyone impersonate releases).
      # Preferred: store the keystore base64-encoded in the
      # ANDROID_KEYSTORE_BASE64 secret; the file is then decoded into a temp
      # path and exported as ANDROID_KEYSTORE_PATH for Gradle.
      # Alternative (advanced): keep the file in private storage and point
      # ANDROID_KEYSTORE_PATH directly at it.
      - name: Reconstruct release keystore
        shell: bash
        run: |
          if [ -n "${{ secrets.ANDROID_KEYSTORE_BASE64 }}" ]; then
            echo "${{ secrets.ANDROID_KEYSTORE_BASE64 }}" | base64 -d > "$RUNNER_TEMP/finvo-release.jks"
            chmod 600 "$RUNNER_TEMP/finvo-release.jks"
            echo "ANDROID_KEYSTORE_PATH=$RUNNER_TEMP/finvo-release.jks" >> "$GITHUB_ENV"
          fi

      - name: Build Universal Release APK
        run: flutter build apk --release
        env:
          # Prefer the base64-decoded path set by the reconstruct step; fall
          # back to a directly-configured ANDROID_KEYSTORE_PATH secret.
          ANDROID_KEYSTORE_PATH: ${{ env.ANDROID_KEYSTORE_PATH || secrets.ANDROID_KEYSTORE_PATH }}
          ANDROID_KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          ANDROID_KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
          ANDROID_KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}

      - name: Verify APK signature
        shell: bash
        run: |
          APK_PATH="build/app/outputs/flutter-apk/app-release.apk"
          # apksigner is bundled with the Android SDK present on the runner.
          if [ -x "$ANDROID_HOME/build-tools/"*/apksigner ]; then
            APKSIGNER=$(ls "$ANDROID_HOME/build-tools/"*/apksigner | head -1)
            "$APKSIGNER" verify --print-certs "$APK_PATH"
          else
            echo "::warning::apksigner not found, skipping signature verification"
          fi

      - name: Prepare Android Release Artifact
        run: |
          TAG_NAME="${GITHUB_REF_NAME:-v0.0.0}"
          APK_PATH="build/app/outputs/flutter-apk/app-release.apk"
          OUTPUT_APK="finvo-${TAG_NAME}-android.apk"

          mkdir -p release_artifacts
          cp "$APK_PATH" "release_artifacts/$OUTPUT_APK"

          # Extract version changelog from root CHANGELOG.md if available
          python3 -c "
          import re, os
          changelog_path = '../CHANGELOG.md'
          tag = '${TAG_NAME}'.lstrip('v')
          notes = ''
          if os.path.exists(changelog_path):
              with open(changelog_path, 'r', encoding='utf-8') as f:
                  content = f.read()
              match = re.search(r'## \[' + re.escape(tag) + r'\][^\n]*\n(.*?)(?=\n## \[|\Z)', content, re.DOTALL)
              if match:
                  notes = match.group(1).strip()
          with open('release_artifacts/RELEASE_NOTES.md', 'w', encoding='utf-8') as f:
              f.write(notes)
          "

      - name: Upload Android Artifact
        uses: actions/upload-artifact@v4
        with:
          name: android-release-apk
          path: client/release_artifacts/

  # ============================================================================
  # iOS Release Build (Sideload IPA)
  # ============================================================================
  ios:
    name: iOS Release (Sideload IPA)
    runs-on: macos-latest
    defaults:
      run:
        working-directory: ./client

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Flutter SDK
        uses: subosito/flutter-action@v2
        with:
          channel: ${{ env.FLUTTER_CHANNEL }}
          cache: true

      - name: Install Dependencies
        run: flutter pub get

      - name: Build iOS (no codesign)
        run: flutter build ios --release --no-codesign

      - name: Create IPA for Sideloading
        run: |
          TAG_NAME="${GITHUB_REF_NAME:-v0.0.0}"
          OUTPUT_IPA="finvo-${TAG_NAME}-ios-sideload.ipa"

          cd build/ios/iphoneos
          mkdir -p Payload
          cp -r Runner.app Payload/
          zip -r "../../$OUTPUT_IPA" Payload

          mkdir -p ../../../release_artifacts
          cp "../../$OUTPUT_IPA" "../../../release_artifacts/$OUTPUT_IPA"

      - name: Upload iOS Artifact
        uses: actions/upload-artifact@v4
        with:
          name: ios-release-ipa
          path: client/release_artifacts/

  # ============================================================================
  # Unified Release Publishing Step
  # ============================================================================
  publish-release:
    name: Publish Unified GitHub Release
    needs: [android, ios]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Download Android Artifact
        uses: actions/download-artifact@v4
        with:
          name: android-release-apk
          path: release_artifacts

      - name: Download iOS Artifact
        uses: actions/download-artifact@v4
        with:
          name: ios-release-ipa
          path: release_artifacts

      - name: Create or Update GitHub Release with both Android & iOS Binaries
        uses: softprops/action-gh-release@v2
        with:
          files: release_artifacts/*
          tag_name: ${{ github.ref_name }}
          name: Release ${{ github.ref_name }}
          body_path: release_artifacts/RELEASE_NOTES.md
          draft: false
          prerelease: ${{ contains(github.ref_name, 'alpha') || contains(github.ref_name, 'beta') }}
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
